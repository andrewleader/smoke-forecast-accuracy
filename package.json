{
  "name": "smoke-forecast-accuracy",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "collect:forecasts": "node --env-file-if-exists=.env --import tsx scripts/collectForecasts.ts",
    "collect:actuals": "node --env-file-if-exists=.env --import tsx scripts/collectActuals.ts",
    "score": "tsx scripts/score.ts",
    "run:daily": "npm run collect:forecasts && npm run collect:actuals && npm run score",
    "check": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "tsx": "^4.19.0",
    "typescript": "^5.7.0"
  }
}